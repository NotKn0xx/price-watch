name: Price watch - Celulares

on:
  schedule:
    - cron: "30 * * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: price-watch-celulares-run
  cancel-in-progress: false

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - run: pip install -r requirements.txt

      - run: python run.py
        env:
          PROFILE: celulares
          DB_PATH: price_watch_celulares.db
          TELEGRAM_BOT_TOKEN: ${{ secrets.CELULARES_TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

      - name: Guardar historial de precios
        run: |
          git config user.name "price-watch-bot"
          git config user.email "actions@users.noreply.github.com"
          git add price_watch_celulares.db
          git diff --cached --quiet || git commit -m "Actualiza historial de precios [celulares]"
          git push
